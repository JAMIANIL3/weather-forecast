# Example environment variables for this app (do NOT put secrets here)
# Copy this to .env and fill with real values.

RAILS_ENV=development

# Weather provider API key
OPENWEATHER_API_KEY=API_KEY_HERE

# Database (for local sqlite)
DATABASE_URL=sqlite3:db/development.sqlite3

# Host/port
HOST=localhost
PORT=3000
