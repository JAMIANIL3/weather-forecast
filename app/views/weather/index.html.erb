<h2>Weather Forecast</h2>

<%= form_with url: forecast_path, method: :get, data: { turbo_frame: "forecast_frame" } do %>
  <div style="display:flex;gap:8px;">
    <%= text_field_tag :address, params[:address], placeholder: "Enter City or ZIP", style: "flex:1;padding:10px;" %>

    <%= select_tag :units,
      options_for_select(
        [["°C", "metric"], ["°F", "imperial"]],
        params[:units] || "metric"
      ),
      style: "padding:10px;"
    %>

    <%= submit_tag "Search", style: "padding:10px 16px;" %>
  </div>
<% end %>

<hr>

<turbo-frame id="forecast_frame">
  <p style="color:gray;">Enter an address to see the weather 🌦️</p>
</turbo-frame>
